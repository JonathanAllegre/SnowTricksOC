<!-- NAV BARR TOP-->
<nav class="navbar navbar-expand-lg navbar-dark navbBlack" id="navTop">
    <a class="navbar-brand" href="#"><i class="fas fa-snowflake"></i><span id="TitleNav"> SnowTricks</span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="{{ path('app_home_index') }}" class="nav-link"></i> Figures</a>
            </li>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <a href="{{ path('logout') }}"  class="nav-link"></i> Ajouter une Figure</a>
                </li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') == false %}
            <li class="nav-item">
                <a href="{{ path('app_user_register') }}" class="nav-link" ></i> S'enregistrer</a>
            </li>
            {% endif %}
            <li class="nav-item">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ path('logout') }}"  class="nav-link"></i> Se déconnecter</a>
                {% else %}
                <a href="{{ path('app_user_login') }}"  class="nav-link"></i> Se connecter</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>