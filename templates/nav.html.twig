<!-- NAV BARR TOP-->
<nav class="navbar navbar-expand-lg navbar-dark navbBlack" id="navTop">
    <a class="navbar-brand" href="#"><i class="fas fa-snowflake"></i><span id="TitleNav"> SnowTricks</span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="{{ path('home') }}" class="nav-link"></i> Accueil</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('user_registration') }}" class="nav-link" ></i> S'enregistrer</a>
            </li>
            <li class="nav-item">
                <a class="nav-link"  data-toggle="modal" data-target="#registerModal" ></i> Figures</a>
            </li>
            <li class="nav-item">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ path('logout') }}"  class="nav-link"></i> Se déconnecter</a>
                {% else %}
                <a href="{{ path('login') }}"  class="nav-link"></i> Se connecter</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>